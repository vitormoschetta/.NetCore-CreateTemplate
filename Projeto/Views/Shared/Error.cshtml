@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
}


<h1 class="text-danger">Erro</h1>
<h3 class="text-danger">Ocorreu um erro inesperado durante o processamento do seu pedido.</h3>
<h5 class="text-danger">Informe o administrador do sistema se possível.</h5>

@if (Model.ShowRequestId)
{
    <p>
        <strong>Request ID:</strong> <code>@Model.RequestId</code>
    </p>
}
